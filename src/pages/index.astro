---
import { getCollection } from "astro:content";
import { Image } from "astro:assets";

import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Body from "../components/Body.astro";
import Content from "../components/Content.astro";
import Footer from "../components/Footer.astro";
import Masthead from "../components/Masthead.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";

const posts = (await getCollection("posts")).sort(
  (a, b) => a.data.createdAt.valueOf() - b.data.createdAt.valueOf(),
);
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <Body>
    <Header title={SITE_TITLE} />
    <main class="pt-[76px]">
      <Masthead />
      <Content className="">
        <section>
          <p class="mb-8 indent-4">
            Este blog foi concebido com o propósito de compartilhar meu setup e
            aprimorar minhas habilidades com algumas tecnologias recentes.
            Atualmente, atuo como desenvolvedor de software, mais
            especificamente como Full Stack. Nas horas vagas, gosto de fingir
            que sou gamer 😂. Dessa forma, a maior parte do meu dia é passada
            diante do computador, tornando meu setup uma parte crucial da minha
            rotina. Além disso, vejo este projeto como uma oportunidade
            empolgante para acompanhar a evolução das minhas habilidades como
            desenvolvedor e do meu próprio setup. Gostaria de registrar que
            minha inspiração para iniciar esse projeto veio do desenvolvedor
            indie
            <a
              class="text-purple-500"
              href="https://github.com/craftzdog"
              target="_blank"
            >
              Craftdogz
            </a>, que mantém o canal <a
              class="text-purple-500"
              href="https://www.youtube.com/@devaslife"
              target="_blank"
            >
              devaslife
            </a>.
          </p>
        </section>
        <section>
          <ul class="grid grid-cols-1 md:grid-cols-2 gap-4">
            {
              posts.map(post => (
                <li class="text-center mb-4">
                  <a href={`/posts/${post.slug}`}>
                    <Image
                      class="border border-slate-300 dark:border-zinc-700 rounded-xl"
                      src={post.data.heroImage || ""}
                      width={720 * 2}
                      height={500 * 2}
                      alt="Thumbnail"
                    />
                    <div class="mt-3 text-xl">{post.data.title}</div>
                    <div class="opacity-70">{post.data.description}</div>
                  </a>
                </li>
              ))
            }
          </ul>
        </section>
      </Content>
    </main>
    <Footer />
  </Body>
</html>
